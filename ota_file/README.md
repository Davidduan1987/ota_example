# OTA 固件文件

本文件夹用于存放 `lbs_OTA` 工程的 OTA 新固件包。

当前 OTA 文件：

```text
NEW_application.zip
```

该 OTA 文件中的固件版本：

```text
1.1.0+0
```

仓库中 `lbs_OTA/` 目录上传的是 `1.0.0+0` 版本工程源码，`NEW_application.zip` 是已经改好的 `1.1.0+0` 新版本固件，用于蓝牙 OTA 升级测试。

该固件包可用于 `nRF Connect` 或 `nRF Device Manager` APP 的蓝牙 DFU 测试。
